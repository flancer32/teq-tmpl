# Architecture Documentation

- Path: `ctx/docs/architecture/AGENTS.md`
- Template Version: `20260702`
- Changed: `20260925`

This level refines the product lifecycle into stable runtime responsibilities and constraints. Read `overview.md` for the component and state boundaries, `behavior.md` for execution and result behavior, `constraints.md` for invariants and approval boundaries, and `decisions.md` for choices and rejected alternatives. Put implementation paths and test evidence in code and verification documentation.
